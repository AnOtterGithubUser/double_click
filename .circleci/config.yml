version: 2
jobs:
  test:
    working_directory: ~/double_click
    docker:
      - image: circleci/python:3.7.7
    steps:
      - checkout
      - run: sudo pip install -r requirements.txt
      - run: python -m pytest